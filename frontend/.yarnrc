# Use public npm registry
registry "https://registry.npmjs.org/"

# Disable strict SSL (for compatibility)
strict-ssl false

# Network timeout
network-timeout 60000

# Allow Yarn to fallback when packages are missing
unsafe-perm true
